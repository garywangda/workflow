# 流程产品设计：第三轮运行与处理闭环

状态：讨论稿 v0.1。仅软件功能与交互设计，不涉及代码，不代表现有能力已经实现。第一、二轮仍有待确认事项，本稿用于提前讨论，不将前轮建议视为已批准。

## 1. 本轮目标与三轮之间的关系

- 第一轮：用户如何组织、查找和打开流程文件。
- 第二轮：用户如何选择节点、配置人员、数据、分支和时间规则。
- 第三轮：用户如何发起一次业务、完成其中的工作、了解进展、处理异常，并明确这一次业务何时结束。

本轮需要定义运行对象、状态、操作和信息关系。任务详情会细化到可办理的程度，但独立 Task 管理器、看板和甘特图不设计完整页面。Group 的可见范围继续暂缓；仅记录真实运行所必需的最小人员操作边界。

本轮所有案例均为通用产品设计示例，不宣称来自此前业务 PDF。

## 2. 先统一四个对象

| 对象 | 是什么 | 示例 | 与其他对象的关系 |
| --- | --- | --- | --- |
| 流程文件 | 用户维护的业务流程定义，包含草稿和发布信息 | 物品申请 | 可以关联多个 Group，可以多次发布 |
| 发布版本 | 一份可用于运行的确定规则 | 物品申请 v3 | 多次运行可使用同一个版本 |
| 流程运行 | 按某个版本发起的一次具体业务 | 王明的办公用品申请 | 有自己的数据、进展、任务、时间和结果 |
| 待办任务 | 某次运行中要求人处理的一项工作 | 审核王明的申请 | 指向运行、节点及当前处理轮次 |

同一流程加入两个 Group，不产生两份发布版本或两次运行。Group 重命名、删除关联不改变已发生的业务运行。

同一节点在退回重提后可以产生新的处理轮次。旧任务和旧审批意见保留，不能用一条不断覆盖的记录表达多次办理。

## 3. 第三轮需要的页面和入口

| 位置 | 建议内容 | 本轮界限 |
| --- | --- | --- |
| 流程库 | 打开设计、更多菜单中的“发起流程”“查看运行” | 文件列表继续使用发布状态，不混入任务完成状态 |
| 编辑器顶部 | 保存状态、检查流程、模拟、发布 | “模拟”和“真实发起”明确分开 |
| 某流程的运行列表 | 运行标题、编号、状态、发起人、开始时间、当前步骤 | 只查看该流程的运行，不扩展成全产品运营仪表盘 |
| 运行详情 | 进展、当前待处理事项、业务数据、处理记录 | 只读流程图配合业务摘要，不使用可编辑画布冒充进展页 |
| 任务详情 | 办理说明、所需数据、表单、完成或审批操作 | 可从运行详情进入；未来 Task 管理器复用此详情 |

运行详情属于业务查看页面，建议显示全局导航；只有流程设计器隐藏全局导航。运行图内不出现添加节点工具，避免用户误改设计。

从流程库进入运行列表再返回，应恢复原有 Group、搜索和滚动位置。运行列表里的 Group 分类如需展示，首版建议沿用流程文件的当前关联，并明确它不是历史归属记录。

## 4. 从草稿到真实发起

### 4.1 发布前

允许未完成配置的草稿保存。发布前检查启动入口、可达路径、必填配置、人员来源、字段引用、分支出口和结束结构。

检查结果区分阻断问题与建议项，点击后定位对应节点和字段。发布不是保存按钮的另一种名称。

建议发布后形成明确版本标识；新编辑进入草稿。正在运行的业务继续使用启动时的版本，不自动套用新规则。首版不提供运行中换版本。

### 4.2 手动发起

建议流程：选择已发布流程 → 填写启动信息 → 查看发起摘要 → 提交 → 打开运行详情。

简单表单可将摘要和提交放在同一页，不强制增加独立确认页。页面必须让用户知道这是发起真实业务。

| 元素 | 建议规则 |
| --- | --- |
| 流程及版本 | 展示将使用的版本；提交时版本若已变化，不静默切换 |
| 运行标题 | 提供可读默认值，例如“物品申请 · 王明 · 09月12日”，可按配置由业务字段生成 |
| 启动字段 | 使用发布版本的表单；即时提示必填和格式问题 |
| 发起人 | 记录实际操作人；代他人发起作为后续独立规则，不隐含支持 |
| 提交中 | 防止重复点击；显示正在创建 |
| 结果不确定 | 先核实是否已有运行，再决定是否重试，不能直接创建第二份 |
| 发起成功 | 显示运行编号、状态和下一步，不仅出现短暂成功提示 |

用户开始填表后发布了新版本，建议保留其已打开版本，提交前明确提示并重新检查该版本是否仍允许启动；旧版本停止启动时保留输入，引导迁移并校验不兼容字段。最终采用哪种版本选择策略需要讨论。

### 4.3 定时与事件发起

定时发起必须能查看计划是否启用、时区、下一次时间，以及最近一次触发结果。停用计划只阻止后续发起，不取消已有运行。

事件发起必须能区分“收到事件但条件不匹配”“创建成功”“数据无效”“疑似重复”。自动发起失败需要有可定位的记录和处理入口，不能因为没有人工点击就无从发现。

计划错过、系统恢复后的补发，以及同类运行尚未结束时是否继续发起，仍需明确配置规则；不能由软件悄悄选择。

## 5. 运行状态与节点状态

### 5.1 运行级状态建议

| 状态 | 含义 | 用户需要看到什么 |
| --- | --- | --- |
| 进行中 | 至少还有可推进的业务，或正常等待人/时间/事件 | 当前办理人、等待条件或自动执行步骤 |
| 需要处理 | 某一步遇到阻碍，需要指定人员介入 | 阻碍原因、影响范围、可执行操作 |
| 已完成 | 按正常结束规则结束 | 业务结果和完成时间 |
| 已终止 | 通过业务提前结束或授权取消结束 | 具体原因、操作来源、未完成任务的处理结果 |
| 执行失败 | 已按规则停止执行，不能继续自动推进 | 失败位置、已产生结果和后续处理建议 |

“需要处理”不一定表示所有并行分支都停止。详情必须显示哪些分支仍在工作，不能仅靠一个总状态描述整张图。

业务“拒绝”是审批结果，不自动等于执行失败；如果拒绝后还要通知，运行此时仍可进行。发起人主动取消与流程配置的提前结束都可归入已终止，但必须保留不同的原因与来源。

首版建议不增加通用“暂停/恢复整条流程”按钮。暂停涉及正在调用的外部服务、计时器和已派发任务，需有真实需求后单独定义。

### 5.2 节点级状态建议

尚未到达、执行中、待领取、待办理、等待时间、等待事件、已完成、需要处理、执行失败、已取消、本轮未走此路径。

只显示适合该类型的状态。尚未到达与已经确认不会走到的分支应可区分；存在重提轮次时，“本轮未走此路径”不代表永远不可达。

颜色之外同时提供文字和图形标识。建议先展示“当前等待审批”等具体摘要，不默认显示难以准确解释的流程百分比。

## 6. 运行详情布局

建议顶部固定显示运行标题、编号、流程/版本、状态、发起人和时间，并提供当前用户可执行的操作。

下方采用三个内容页签：

1. **进展**：当前事项摘要、只读流程图、选中节点的处理详情。默认聚焦当前步骤，多分支时可查看多个活跃步骤。
2. **业务数据**：按表单或业务分区展示可查看的数据。详情查看不是任意修改业务数据的入口。
3. **处理记录**：发起、领取、提交、审批、退回、重提、转交、失败、重试、结束等时间顺序记录。

节点详情应回答：谁处理、何时开始、正在等什么、使用了什么内容、产生什么结果、是否重试或重提。高级技术错误详情折叠，普通用户优先看到可理解的原因与下一步。

从旧节点查看表单时，默认查看该次提交或审批所依据的记录，并明确与当前业务值的区别。

## 7. 人工任务的办理闭环

### 7.1 任务详情组成

| 区域 | 内容 |
| --- | --- |
| 标题信息 | 任务名称、所属运行、任务状态、截止时间 |
| 办理说明 | 要完成什么、检查项、相关附件及说明 |
| 业务上下文 | 完成本步需要的上游数据与相关记录 |
| 本步表单 | 可编辑字段、只读字段、必填要求、完成证明 |
| 操作区 | 领取、保存草稿、提交完成，或审批通过/拒绝/退回 |
| 处理记录 | 本任务当前轮次及关联的历史轮次 |

字段不可见时，不能在任务摘要、通知或附件预览中间接泄露。具体权限体系后续展开，但所有入口应使用一致的字段可见规则。

### 7.2 单人、候选领取与多人

- 指定单人：直接进入该人的待办理状态。
- 多个候选人：先待领取；一个人成功领取后，其他人不能再提交同一份任务。
- 多人分别处理：每个人有自己的处理记录，节点按配置汇总完成；是否首版支持仍依第二轮决定。

候选任务领取只是确定办理人，不等于业务完成。领取后转交或释放领取需要单独授权，并记录原因。

多人同时打开任务，状态变更后页面应提示更新。已经被他人处理或取消的任务不能继续提交；保留本地输入便于复制或查看，不以旧页面覆盖新结果。

### 7.3 保存与提交

保存草稿不完成任务，不触发后续节点。草稿何时对其他人可见需要讨论，建议普通个人任务的未提交输入不作为下游正式数据。

提交时再次检查任务是否有效、人员是否有权办理、字段是否满足要求。成功后显示明确结果和返回入口；重复提交不产生第二份完成结果。

## 8. 审批、退回与重新提交

审批页同时展示当前决策对象、关键字段、附件和必要历史意见。审批意见是否必填按决策分别设置，建议拒绝和退回要求理由。

通过和拒绝走配置的业务路径。退回修改需展示目标步骤、接收人和原因，让办理人知道具体要改什么。

建议退回后：

1. 原审批轮次标记为已退回，保留已产生的意见。
2. 为指定修改步骤创建新的办理轮次，关联原运行。
3. 修改人只能修改该步骤允许的字段，提交时记录变更。
4. 重新进入审批时创建新轮次，并显示变更摘要与历史意见。

多人审批中的退回如何影响其他人的未完成审批必须确定。建议首版采用“一人退回则关闭本轮其余待审批任务，修改后全部重审”；这只是易解释的起点，需用户确认。

暂不将“撤回已提交任务”“撤销某人的通过意见”“发起人取消整次业务”合并成同一个撤回按钮。它们的影响范围不同。

## 9. 等待、逾期与提醒

运行详情必须区分三种等待：等人处理、等时间到达、等事件发生。

| 情况 | 展示 | 建议行为 |
| --- | --- | --- |
| 人工任务临近截止 | 截止时间、办理人 | 按已配置规则提醒，不重复轰炸 |
| 人工任务逾期 | 已逾期时长 | 默认仍可办理；升级或取消需明确规则 |
| 时间等待 | 预计恢复时间、时区 | 到期自动继续；不显示人工“完成”按钮 |
| 事件等待 | 所等事件、业务关联摘要、最长等待 | 匹配后继续，超时走配置的处理方式 |
| 外部动作等待结果 | 请求已发出/等待确认/超时 | 超时不能自动推断外部动作没有执行 |

首版建议运行中的已有截止时间不因流程草稿修改而变化。若允许单次延长截止，应授权、填写原因并保留原时间。

催办只是发送提醒，不改变截止、不自动转交任务、不提高审批权限。是否在运行页提供催办及频率限制需要讨论。

## 10. 异常处理与重试

| 问题 | 用户看到的说明 | 建议操作 |
| --- | --- | --- |
| 找不到办理人 | 哪一步的人员来源无法得到有效人员 | 指定授权处理人，重新分配并记录原因 |
| 缺少或无效数据 | 哪个字段阻止继续及其来源 | 通过明确的补充任务修正，不能随意篡改历史提交 |
| 通知发送失败 | 渠道与失败原因 | 安全时重试；是否阻塞业务取决于节点配置 |
| 外部服务失败 | 动作、尝试次数、已知执行结果 | 可安全重复时有限重试；未知结果先核实 |
| 等待事件超时 | 等待内容、开始时间和超时规则 | 走超时分支，或由授权人员继续等待 |
| 并行分支失败 | 失败分支及其他分支仍在进行/已完成的状态 | 处理失败分支，不默认重新执行已完成分支 |

自动重试展示次数和下次时间；人工重试展示影响范围。重试不删除前次失败记录，也不把整条业务从头发起。

不建议首版提供任意“跳过节点”“强制通过审批”“改成已完成”操作。若确有业务需要，必须单独定义允许的类型、人员、原因及影响，不能成为万能修复按钮。

## 11. 结束、取消、归档与历史

- 正常结束展示结果摘要和实际完成时间；完成不代表全部可能分支都执行过。
- 提前结束或授权取消后，尚未完成的任务关闭，后续时间/事件不再推进该运行。
- 正在进行的外部动作可能无法撤销，详情应记录最终可确认结果。结束流程不承诺自动退款、撤回通知或恢复外部记录。
- 已完成运行首版建议不直接重新打开；再次办理应新建运行，必要时引用原运行。
- 归档流程文件与停止新启动分开表达。建议归档确认时明确是否停用定时/事件触发；已有运行继续，历史仍可查看。
- 不提供一键永久删除流程及全部运行记录。保留期限和删除权限属于后续治理设计。

“取消运行”谁能操作、在哪个阶段还能取消，以及取消是否需要理由，是本轮关键待决事项。建议不要默认发起人永远能取消已经发生外部动作的业务。

## 12. 最小操作权限边界

这里描述能力，不预设完整部门体系或 Group 可见范围：

| 能力 | 最小边界 |
| --- | --- |
| 编辑流程 | 修改草稿，不自动获得发布或审批权 |
| 发布流程 | 决定可用于新运行的版本 |
| 发起运行 | 使用指定流程开始业务 |
| 查看运行 | 查看被允许的业务及字段，不默认看到所有人的运行 |
| 办理任务 | 处理分配/领取的有效任务及授权字段 |
| 处理异常 | 仅执行被授权的重分配、重试等操作，并记录行为 |
| 取消运行 | 按业务阶段和影响限制，不与普通查看权限绑定 |

同一人可以具备多种能力，但“流程维护负责人”不自动等于所有业务审批人。“发起人不能审批自己的申请”等职责分离规则如需要，应作为明确配置，不默认套用所有行业。

## 13. 与未来 Task、看板、甘特图的关联

本轮只确定共用对象，避免未来形成互不一致的任务副本：

- Task 管理器聚合运行产生的待办，打开同一任务详情；在不同入口提交都更新同一任务。
- 看板未来可以展示运行或任务，但一个视图必须明确卡片代表哪个对象，不能混用。
- 工作流驱动的状态不能默认通过拖拽到“完成”绕过表单和审批。未来拖拽若触发业务操作，应遵循相同校验。
- 甘特图将来展示计划时间与实际时间，需要区分设计时的预期任务和运行时真正生成的任务。
- 第二轮配置的截止、人员、依赖和完成规则为这些模块提供语义基础；本轮不设计排期、资源负载和自定义看板列。

## 14. 第三轮 Figma 页面清单

建议按以下顺序制作，当前仅列设计任务，尚未生成本轮画面：

1. 真实发起表单：输入、校验错误、提交中、成功及结果不确定。
2. 某流程的运行列表：进行中、需要处理、已完成等状态及筛选。
3. 运行详情：当前事项、只读流程图、节点记录和业务数据。
4. 人工任务详情：待领取、已领取可编辑、提交成功、已被他人处理。
5. 审批详情：通过/拒绝/退回，以及修改后再次审批。
6. 异常详情：人员缺失、外部结果未知、重试记录。
7. 结束结果：正常完成、业务终止、取消后的未完成任务说明。

无需为每种状态单独做完整页面；可用同一主页面的关键状态变体表达。

## 15. 需要讨论的事项

### 首先决定

1. 本轮是否接受“运行列表 → 运行详情 → 任务办理”的信息结构？建议先在每份流程下进入运行列表，后续再做跨流程聚合。
2. 第一批运行是否只开放手动发起，还是定时、事件必须同时可用？预设存在不意味着真实运行可暂时装作支持。
3. 最小人员模式需要单人、候选领取、多人各自完成中的哪些？与第二轮保持同一决定。
4. 多人审批中一人退回，是否关闭本轮其他审批并全部重审？建议先采用该规则，例外需明确。
5. 谁能取消运行、转交任务、延长截止和处理异常？建议分别授权，不提供万能管理员业务操作。

### 随后细化

- 标题和编号是否需要用户定义格式？建议先稳定编号与可读标题分开。
- 发起人可以看到多少审批意见和内部处理记录？不默认全部公开。
- 是否需要催办、站内通知及其他渠道？先确定实际接收入口，再画通知偏好。
- 计划错过后的补发策略、事件提前到达与重复事件规则是什么？
- 运行结束后是否允许补充附件或备注？建议允许授权追加记录，不修改原结果。
- 归档与停用新启动采用怎样的明确交互？
- 是否需要工作日历、代理办理、运行暂停或历史版本迁移？没有明确需要时暂缓。

### 前轮尚未确认，继续作为依赖

15 个具体预设、并行结构、字段与表单模型、明细表、角色来源、退回目标限制、模拟与发布原则。第三轮描述这些能力时采用建议方案，并不代表已经完成决策。

## 16. 分阶段设计顺序与最小闭环

### A. 最小可评审闭环

手动发起 → 单人填写或办理 → 单人审批 → 按结果正常/提前结束。

必须同时能够查看运行进展、打开并提交任务、保留意见与数据记录、区分草稿和发布版本，以及发现人员缺失和提交失败。这是通用产品的设计验证范围，不声称已经覆盖某份业务 PDF 的完整流程。

### B. 常见业务完整性

加入条件分支、退回重提、候选领取、截止提醒、通知与人工异常处理。根据实际需求加入明细表和多人审批规则。

### C. 自动化与复杂协作

加入定时/事件发起、时间/事件等待、外部服务、并行汇合及其异常边界。若真实首个业务必须使用其中某项，就提前到 A 或 B，而不是删掉业务要求。

Task 聚合、看板、甘特图及复杂权限治理留到后续轮次。

## 17. 本轮评审场景

1. 同一流程发起两次，数据、审批和进展互不串用；从不同 Group 入口发起遵循同一发布规则。
2. 填写草稿未提交时，下游不启动；有效提交后只完成一次任务。
3. 两人同时领取，只有一人成功；另一人的旧页面不能越权提交。
4. 审批退回后产生修改及复审轮次，旧意见仍可追溯。
5. 任务逾期仍可按规则办理，不自动被当作拒绝或系统失败。
6. 外部服务结果未知时，用户不会被引导盲目重复提交。
7. 一个并行分支失败时，可以看到其他分支的真实状态；重试不重做已经成功的工作。
8. 编辑并发布新版本，不改变已有运行的规则。
9. 取消后旧任务不可继续提交，已经发生的外部动作不被伪装为已撤销。
10. 用户可以解释一次业务当前由谁处理、在等什么、出了什么问题、下一步能做什么，以及最终为什么结束。

上述场景用于评审设计完整性，不是代码测试清单，也不代表相关功能已经存在。
