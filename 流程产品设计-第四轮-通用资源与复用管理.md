# 流程产品设计：第四轮通用资源与复用管理

状态：讨论稿 v0.1。仅产品功能和交互设计，不涉及代码，不代表已有功能或已批准的最终方案。前三轮未确认的内容继续作为依赖。

## 1. 本轮定位

第一轮解决流程文件的组织；第二轮解决节点配置；第三轮解决一次业务的运行与处理。第四轮讨论：当用户拥有多份流程时，哪些内容应统一管理、哪些内容可以复用，以及修改公共内容会影响谁。

本轮目标是使产品保持通用并减少重复配置，而不是立即增加一套复杂后台。公共资源按实际需要逐步启用，简单流程仍可直接在编辑器中完成配置。

Task 管理器、看板、甘特图不展开页面设计。Group 个人/共享范围继续暂缓；不按部门预置 Group，也不将分类关系解释为授权关系。

## 2. 首先区分三种复用关系

| 关系 | 含义 | 修改是否同步 | 建议适用对象 |
| --- | --- | --- | --- |
| 同一对象的多个入口 | 多个位置打开同一份内容 | 同步，因为本来就是同一对象 | 流程关联多个 Group |
| 从模板复制 | 以现成内容创建独立对象 | 不同步；后续各自修改 | 流程模板、表单模板 |
| 引用公共资源 | 使用统一管理的资源 | 按资源类型和版本规则生效 | 业务角色、连接、工作日历 |

界面使用明确动词：“添加到 Group”“从模板创建”“选择连接”。避免用一个“关联”按钮混淆三种行为。

第一版建议流程和表单模板都以复制为主，不提供修改模板后批量覆盖所有使用者的功能。公共连接和角色则确有持续引用价值，但必须定义变更规则。

## 3. 通用资源范围与优先级

| 资源 | 解决什么问题 | 建议首版范围 | 暂缓内容 |
| --- | --- | --- | --- |
| 人员目录 | 节点可以选择有效人员 | 搜索、基本身份、启用状态 | 完整组织架构、人事系统 |
| 业务角色 | 多个流程使用同一种职责 | 用户定义角色、配置成员、节点引用 | 自动按复杂组织关系推导人员 |
| 表单模板 | 重用常见字段和布局 | 保存模板、从模板复制、字段映射 | 全局表单修改自动覆盖运行 |
| 流程模板 | 重用一套流程结构 | 从已有流程生成模板、创建独立流程 | 模板市场、跨组织分享、自动升级 |
| 外部连接 | 多节点共用服务连接 | 创建、验证、选择、停用、使用位置 | 任意服务自动识别、连接市场 |
| 工作日历 | 统一工作日和节假日 | 业务要求工作日计时后再引入 | 自动排班、人员产能调度 |
| 通知内容模板 | 复用提醒和通知文案 | 先在节点内编辑；频繁重复后再提供模板 | 多语言消息运营平台 |

这些不是七个必须立即建设的独立模块。建议先完成运行所必需的人员和连接管理，其余按重复配置的真实成本决定。

## 4. 入口与导航的组织

建议全局“设置”下提供“人员与角色”“连接”等管理入口，避免新增一个无所不包的常驻资源导航。

流程模板保留在流程库的“新建流程”路径中；表单模板保留在表单编辑器的“从模板添加”路径中。若将来数量明显增加，再提供统一模板管理页。

编辑器内选择公共资源时可以查看摘要和使用状态。具备创建能力的用户可通过明确入口打开创建面板，完成后回到原节点并选中；不能因为用户输入一个名称就自动创建公共资源。

进入完整管理页前保留草稿与编辑位置，返回后恢复。没有管理能力的用户仍应能选择自己被允许使用的有效资源；“能使用”和“能修改配置”分别表达。

## 5. 人员与业务角色

### 5.1 人员目录

节点选择器展示姓名、辅助识别信息及有效状态。重名人员不能只靠姓名区分。已停用人员保留历史显示，不从审批记录中消失。

人员来源需讨论：首期由产品维护基本用户目录，还是接入已有身份系统。不能先画出完整人员树，却没有确定谁创建和维护人员。

### 5.2 业务角色

角色表达职责，例如申请审核人、设备检查人、项目验收人。角色由用户定义，不内置农业岗位，也不依赖 Group 或部门名称。

建议字段：角色名称、说明、成员、状态。角色成员的维护属于公共资源管理；节点决定如何从解析出的成员中产生任务，例如全部审批或候选领取。

“角色有三名成员”不能自动推断为三人都要完成，也不能自动挑选其中一人。

### 5.3 角色变更何时生效

建议角色在节点实际进入时解析为具体人员，并记录解析结果：

- 新到达的节点使用当时有效成员。
- 已派发的任务保持原办理人，公共角色改动不静默转交任务。
- 原办理人停用时，现有任务进入需要处理的状态，由授权人员重新分配。
- 历史记录保留当时的人员和角色来源，不随着角色改名或换人改写历史事实。

对要求运行全程人员固定的业务，可讨论“启动时确定人员”的替代策略。首版不建议每个节点随意选择多种生效方式，先确定一种可解释的默认行为。

## 6. 表单模板与数据字段

### 6.1 模板包含什么

包含字段类型、标签、帮助文字、布局、静态选项及可复用校验。是否包含默认值、必填和可见规则需按是否依赖具体流程判断。

办理人、特定上游节点引用、外部连接和具体业务数据不应作为可直接使用的通用模板内容。保存模板时提示这些依赖，要求清除或转为待配置项。

### 6.2 应用模板

应用到当前流程时，为字段选择“创建新字段”或“映射到已有字段”。只对类型兼容的字段提供映射建议，不因名字相同就自动当作同一数据。

例如两个模板都包含“备注”，用户可能希望它们分别表示申请说明与验收说明，应允许保持独立。

应用后形成当前流程自己的表单配置。以后修改源模板不影响该流程，更不改变已经提交的数据。

### 6.3 模板编辑和状态

首版可提供名称、说明、更新时间和归档。归档阻止新选用，不影响已经复制出的表单。

建议区分“空白表单结构预览”和“带示例值预览”；真实申请数据、附件、人员意见不作为模板示例保存。

## 7. 流程模板

模板帮助用户建立新流程，不能绕过检查直接成为可运行版本。

从模板创建时建议顺序：选择模板 → 查看用途及包含步骤 → 填写新流程名称和 Group → 创建独立草稿 → 补齐人员、连接及其他必需配置 → 检查并发布。

| 内容 | 建议复制规则 |
| --- | --- |
| 节点、连线、字段、布局 | 复制结构并保持内部引用关系 |
| 名称、说明 | 可以带入建议值，由用户确认 |
| Group | 默认由新流程选择，不把模板分类强制带入 |
| 人员、角色、连接 | 创建前展示依赖，必须确认可用；不静默绑定无权使用的资源 |
| 发布身份和运行记录 | 不复制 |
| 已提交数据、附件、审批意见 | 不复制 |

首版建议在已有流程的更多菜单提供“保存为模板”，但不加入公开市场。模板可归档，已创建流程保持独立。

用户已经可以复制流程，因此是否立即增加模板管理，应根据是否需要清理业务绑定、复用说明和标准起点决定；不能只为增加菜单而做一套重复功能。

## 8. 外部连接与动作目录

### 8.1 连接与节点分工

连接负责识别服务、环境、身份验证和有效性；节点负责本次要执行的动作、输入、输出和失败处理。

例如“通知服务连接”不决定通知谁；通知接收者与内容在节点配置。连接信息不会因为流程被复制就复制出一套明文凭据。

### 8.2 连接管理字段

名称、服务类型、环境标识、身份验证配置、维护人员、状态、最近验证时间及使用位置。普通选择器只展示足以辨认连接的信息，不展示密钥。

连接状态建议：未完成配置、可用、需要重新认证、已停用。最近一次验证成功不保证下一次真实调用必定成功，运行时仍需处理错误。

### 8.3 验证与试用

“验证连接”应清楚说明验证范围，优先验证认证和访问能力。会发送消息、创建记录或产生费用的验证不得伪装成普通连接检查；需要明确展示将执行的动作。

测试环境与正式环境可在名称之外使用固定标识，避免仅靠用户命名区分。模拟流程默认不使用真实动作；真实试运行另行明确。

### 8.4 变更与停用

- 更新身份凭据可以维持同一连接身份，但记录变更人和时间。
- 改为另一个服务、账号或业务环境可能改变动作目标，建议创建新连接并显式替换引用。
- 停用前展示引用它的流程和可能受影响的运行；尚未执行的调用不再使用该连接，转入已定义的异常处理。
- 已经发出的请求不因停用连接就被自动撤销。
- 被引用的连接优先停用，不直接删除，保留历史可解释性。

首批究竟连接哪些外部服务，需要真实业务清单。通用节点界面不等于已经拥有任意系统集成能力。

## 9. 工作日历与时间规则

只在业务需要“工作日”“营业时间”时引入。普通自然时长不要求用户先创建日历。

日历候选字段：名称、时区、每周工作日、每日工作时段、例外日期、生效范围。是否存在跨午夜工作时段、地区节假日自动导入，留待具体需求。

节点的“2 个工作日”必须选择或明确继承某份日历。没有日历时不默认为周一至周五，也不自动采用农业或某国节假日。

建议任务创建时算出实际截止并记录计算依据。修改公共日历影响后续新计算的时间，不静默改变已派发任务的截止；需重新计算时提供影响预览和授权操作。

该规则不等同于定时触发的未来计划：未来触发时间是否随日历更新而变化，需要在计划设置中明确展示并记录。

## 10. 公共资源的变更影响

| 修改对象 | 草稿 | 新到达的运行步骤 | 已产生任务或历史 |
| --- | --- | --- | --- |
| 模板 | 已复制内容不自动变 | 无影响 | 无影响 |
| 角色成员 | 配置预览更新 | 按节点进入时的有效成员解析，待确认 | 原任务不自动转交，历史不改写 |
| 角色名称 | 当前引用标签更新 | 显示当前名称 | 保留当时来源记录 |
| 连接认证 | 引用同一连接 | 后续调用使用有效认证 | 已发出请求需分别确认结果 |
| 工作日历 | 新计算使用更新规则 | 后续截止计算采用更新规则 | 已确定截止保持，除非明确重新计算 |
| 流程草稿 | 当前编辑内容改变 | 已发布运行不自动换规则 | 历史保持 |

公共资源维护页建议提供“使用位置”：被哪些流程引用、是否有已发布版本、是否存在活跃运行。展示范围遵循查看能力；不能为了显示依赖而泄露无权查看的流程名称或业务内容。

首版不必做复杂依赖关系图，清晰列表及操作前的影响摘要即可。

## 11. 最小管理能力边界

分别定义查看资源、使用资源、创建/修改资源、停用资源和查看使用位置的能力。是否合并成少数管理角色，后续根据产品用户结构决定。

Group 不承担这些权限。流程编辑者可以使用某个连接，并不意味着可以查看其身份凭据或停用它。

资源创建和关键变更保留操作者、时间和变更摘要。历史记录对普通业务用户显示到什么程度，需与第三轮的运行查看规则一致。

本轮不设计完整企业权限矩阵，也不重新打开已暂缓的 Group 个人/共享问题。

## 12. 必须覆盖的交互状态

- 没有资源：说明用途，可创建或继续使用本地配置；依赖必需时明确无法发布的原因。
- 搜索无结果：区分无匹配和完全没有资源。
- 无权创建：可以选择被允许使用的资源，不展示必然失败的创建入口。
- 资源停用或失效：保留原引用并提示修复，不静默清空配置。
- 资源被他人修改：展示变更与影响，避免旧页面覆盖新成员或连接配置。
- 模板字段冲突：用户明确选择创建独立字段或映射，不自动合并。
- 操作失败：保留输入，提供重试；结果不确定时先核实是否已经创建。
- 删除存在引用：解释影响，优先提供归档/停用或替换引用。
- 返回编辑器：恢复节点、滚动和草稿，并显示新资源是否已经选中。

## 13. 第四轮 Figma 设计清单

本轮先列待绘制内容，尚未生成这些画面：

1. 节点内资源选择器：搜索、状态、详情摘要、创建入口、失效引用。
2. 人员与角色管理：角色列表、成员编辑、使用位置及变更影响。
3. 表单模板选择：结构预览、字段映射、应用后回到表单。
4. 从流程模板创建：依赖摘要、待补齐项、创建独立草稿。
5. 连接管理：创建、验证结果、选择环境、失效和停用影响。
6. 工作日历：仅在确认需要后绘制规则与例外日期编辑。

模板、角色和连接共用列表、搜索、详情和状态模式，但具体字段与变更语义不同，不做一个万能资源表单。

## 14. 需要讨论的关键问题

### 优先决定

| 问题 | 建议起点 | 影响 |
| --- | --- | --- |
| 公共角色是否首期需要 | 多流程共用职责时保留；简单试用可直接选人 | 人员配置和换人维护成本 |
| 角色什么时候解析 | 节点到达时解析，已派发任务不自动换人 | 运行中的责任归属 |
| 表单复用是否接受复制模式 | 先复制，字段显式映射 | 避免模板修改意外影响业务 |
| 流程模板是否现在做 | 先确认复制流程是否已满足需要 | 避免重复建设管理入口 |
| 首批外部服务有哪些 | 按真实使用场景列出服务及具体动作 | 决定连接字段和运行行为 |

### 接着讨论

- 人员由谁创建，是否已有身份系统可作为来源？
- 是否真的需要工作日计时，涉及哪些时区和例外日？
- 公共资源维护放在设置中是否足够，哪些需要从编辑器快捷创建？
- 连接停用后，对已经排队但未执行的业务如何通知处理人？
- 是否需要区分测试与正式连接，并限制正式动作的试用？
- 是否存在必须统一更新的表单标准？若存在，后续需引入显式版本升级，而非覆盖复制内容。

### 继续暂缓

公开模板市场、跨组织共享、复杂组织继承、完整人事系统、任意服务连接市场、资源自动升级、产能排期、Task/看板/甘特图完整界面、Group 可见范围。

## 15. 分阶段范围

### A. 能直接支持前三轮

基本人员选择、按需的业务角色、实际外部动作所需的连接管理，以及失效引用提示。没有外部动作时不强迫创建连接；没有工作日要求时不强迫配置日历。

### B. 减少重复配置

表单模板、字段映射、流程模板或改进后的复制流程、资源使用位置。以用户能理解复制与引用的区别为验收重点。

### C. 随规模增加的管理能力

工作日历、通知模板、较细的资源使用权限、标准模板的显式升级与变更影响。根据真实需求调整顺序。

## 16. 评审场景

1. 同一流程在两个 Group 中修改同步；从模板创建的两份流程修改互不影响。
2. 两个表单模板都有“备注”字段，用户可以选择独立字段，不被自动合并。
3. 角色换人后，新到达节点使用新成员，已派发任务仍有明确处理归属。
4. 人员停用后历史审批意见仍能识别原操作者，未完成任务有处理入口。
5. 从模板创建流程后，缺失的人员和连接明确列出，不能误认为已经可以发布运行。
6. 停用连接前能够看到授权范围内的影响；已发生的外部动作不被描述为已撤销。
7. 修改工作日历不会无提示改变已派发任务的截止时间。
8. 用户无需先配置全部公共资源，也能完成简单流程的设计与发起。

本轮评审通过的核心是：用户能够说明正在使用的是同一对象、模板副本还是公共引用，并知道一次修改会影响哪些新配置、后续执行和历史记录。
