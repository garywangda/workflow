# 流程产品设计：第二轮节点预设与配置

状态：讨论稿 v0.1。只讨论产品功能、对象关系和交互，不涉及代码或功能已实现的判断。

## 1. 本轮要形成什么结果

将“可以画流程”细化成“用户知道该选什么节点、配置什么内容、配置后会发生什么”。本轮交付：15 个预设候选清单、右侧设置结构、通用设置元素、数据与人员引用关系、关键异常行为，以及下一版 Figma 应补充的界面状态。

第一轮 Figma 是布局提案，并不意味着其中的默认值、审批方式、字段和全部交互都已经确定。本稿的具体 15 项是本轮建议清单；此前确认的是保留 7 类入口、收敛为 15 个核心预设的方向。

## 2. 延续的边界

- 产品通用，行业案例不成为内置字段或固定角色。
- Group 是用户按工作内容创建的分类；流程可多 Group 关联，多个入口指向同一份内容。
- Group 不决定流程数据、审批人或办理权限；其可见范围仍暂缓讨论。
- 编辑器隐藏全局导航；任务、看板、甘特图本轮只预留关联，不展开模块设计。
- 流程文件、一次流程运行、运行产生的任务是不同对象。
- 本轮不审核现有代码，也不根据 Figma 中有按钮就认定功能已经实现。

第一轮尚需收口：流程库默认平铺还是按 Group 分组。建议默认平铺、记住用户最后选择；这不阻碍本轮讨论。

## 3. 预设、节点和设置的关系

一级入口用于寻找功能；预设用于提供合理初始配置；放入画布的节点是用户实际配置的一个步骤。

例如“填写信息”和“执行任务”都是需要人处理的步骤，区别在于初始强调表单还是执行说明。截止时间、指定人员、完成附件等属于设置，不再分别创建预设。

选择预设后，右侧标题使用节点名称，并显示所属类型。建议不允许随意把审批节点直接切换成外部动作；跨类型替换应明确提示哪些配置、输出引用和连线将失效。同类预设切换也不能无提示覆盖用户已填写内容。

## 4. 15 个核心预设候选

| 一级入口 | 预设 | 做什么 | 专属设置 | 完成条件及输出 |
| --- | --- | --- | --- | --- |
| Trigger | 1. 手动发起 | 用户主动开始一次流程，可填写启动信息 | 启动表单、谁可发起、启动标题规则 | 提交有效启动信息后继续；输出发起人、发起时间、启动字段 |
| Trigger | 2. 定时发起 | 在指定时间或周期创建流程运行 | 一次/重复、日期时间、时区、生效范围、已有运行未结束时的处理 | 每次到期产生一次启动；输出计划时间与实际启动时间 |
| Trigger | 3. 事件发起 | 收到选定来源的事件后启动 | 事件来源、事件类型、匹配条件、字段映射、重复事件识别 | 匹配且有效的事件启动；输出事件数据 |
| Human Task | 4. 填写信息 | 指定人员补充或确认数据 | 办理人、表单、可读/可写/必填字段、完成规则 | 有效提交后完成；输出填写结果、提交人和时间 |
| Human Task | 5. 执行任务 | 指定人员按要求完成一项工作 | 办理人、任务说明、检查项、完成证明、截止与提醒 | 满足完成要求并提交；输出完成结果及附件 |
| Approval | 6. 审批 | 人员对指定内容作出业务决定 | 审批人、多人通过规则、审批内容、意见要求、允许的决策 | 输出通过/拒绝/退回等已启用结果、意见及处理人 |
| Action | 7. 发送通知 | 将信息发送给目标接收者 | 接收者、渠道、标题正文、数据引用、发送失败处理 | 渠道接受发送后完成；不将发送成功当作已读 |
| Action | 8. 更新流程数据 | 自动赋值或计算当前流程内的数据 | 目标字段、赋值方式、值/引用/支持的计算、覆盖规则 | 合法写入后完成；输出更新结果 |
| Action | 9. 调用外部服务 | 执行系统外部的动作 | 连接、动作、输入映射、输出映射、超时与重试 | 收到符合成功规则的结果后完成；输出映射后的数据 |
| Logic | 10. 条件分支 | 根据数据选择一条后续路径 | 有顺序的条件组、AND/OR、分支名称、默认路径 | 按优先级命中第一条路径；没有命中走默认路径 |
| Logic | 11. 并行分支 | 同时开展多个步骤并在约定处汇合 | 分支列表、对应汇合点、汇合规则、分支失败处理 | 建议首版所有分支完成后继续；保留各分支结果 |
| Wait | 12. 等待时间 | 暂停直到时间满足 | 等待时长/指定时间/引用日期、时区、时间已过的处理 | 时间满足后继续；输出实际恢复时间 |
| Wait | 13. 等待事件 | 流程运行到此处后等待外部或业务事件 | 事件来源、关联当前运行的字段、匹配条件、最长等待、超时路径 | 收到属于当前运行的匹配事件后继续；输出事件数据 |
| End | 14. 正常结束 | 标记本次运行完成并形成结果 | 结果名称、结果摘要、对外输出 | 结束本次运行，显示完成结果 |
| End | 15. 提前结束 | 按业务规则主动终止本次运行 | 原因类别、原因说明、未完成任务的处理 | 输出业务终止结果；不等同于系统执行失败 |

这份清单覆盖启动、人处理、系统动作、判断、并行、等待和结束。具体是否能稳定保留全部 15 项，需要结合本轮讨论；数量不应压过概念清晰度。

“更新流程数据”不承担跨流程数据库或库存台账管理。修改外部业务记录通过“调用外部服务”表达，避免一个动作暗含整个数据管理系统。

“并行分支”虽然是一个预设，插入时应创建分支起点和配对汇合点；汇合点是其结构组成，不另占一个菜单预设。删除、增加分支以及汇合失败处理需要一起定义。

## 5. 右侧设置的统一结构

建议使用一个可滚动面板和折叠分区，不为每类节点设计完全不同的布局。

| 分区 | 内容 | 何时出现 |
| --- | --- | --- |
| 面板头部 | 节点名称、类型/预设、配置完整状态、更多操作 | 所有节点 |
| 基本信息 | 名称、说明 | 所有节点 |
| 本步要做什么 | 对应预设的核心配置 | 所有节点，但字段随类型变化 |
| 使用的数据 | 可用输入、表单/消息等内容引用、必要映射 | 需要消费数据时 |
| 时间与异常 | 截止、超时、提醒、重试及失败处理 | 按节点能力显示，不强行统一全部字段 |
| 完成后的结果 | 输出字段、业务结果与后续出口说明 | 有输出或分支时 |

用户优先看到完成此步骤所必需的配置；高级设置默认折叠，但启用了高级规则时显示摘要，例如“截止：创建后 2 天 · 提醒 1 次”。

表单设计、复杂条件、长消息和字段映射在侧栏中显示摘要及编辑入口，打开更宽的编辑区域；不要把大表格硬塞进窄侧栏。返回后保留节点选择和画布位置。

未选中节点时，右侧显示流程级信息、数据字段入口和检查结果；选中连线时显示路径名称与该出口的规则；多选节点时只显示批量可安全修改的内容。

## 6. 通用设置元素及关联对象

| 通用元素 | 组成与交互 | 关联什么 | 必须说明的边界 |
| --- | --- | --- | --- |
| 值输入器 | 固定值 / 引用数据；需要计算时才展示计算入口 | 启动数据、流程字段、有效上游输出 | 不把复杂表达式输入框作为普通用户默认入口 |
| 数据选择器 | 按来源分组、搜索、类型标识、字段路径、示例 | 流程数据结构和上游节点输出 | 只能引用当前步骤可获得的数据；可缺失数据必须可处理 |
| 人员选择器 | 固定人员 / 业务角色 / 引用人员字段 | 人员目录、角色、发起人及上游人员字段 | 无匹配、多人匹配必须有处理；Group 不自动成为人员组 |
| 表单选择与编辑 | 字段、布局、必填、可见、可编辑、帮助文字 | 字段定义与该节点的数据读写 | 同名标签不意味着同一字段；只读可见与可编辑是两件事 |
| 条件编辑器 | 左值、运算符、右值、条件组、AND/OR | 类型明确的流程数据 | 数字、日期、文本、人员等提供不同运算符；空值单独处理 |
| 时间规则控件 | 时长 / 具体日期 / 引用日期、时区 | 启动、任务、等待与超时 | 任务截止不等于强制终止；工作日需要单独日历定义 |
| 内容编辑器 | 普通文字、数据变量、预览、缺失值提示 | 通知、任务说明、结果摘要 | 不直接展示内部字段路径作为最终通知内容 |
| 映射编辑器 | 目标字段、来源值、类型校验、必填提示 | 外部动作输入输出、事件数据 | 敏感连接信息由连接对象维护，节点只选择连接 |
| 多人处理规则 | 单人/候选人领取/所有人处理等适用模式 | 人工任务和审批任务 | 人工任务共同完成规则与审批投票规则分开命名 |
| 异常处理控件 | 等待处理、有限重试、超时出口等适用动作 | 当前步骤运行状态与后续路径 | 业务拒绝不自动重试；系统错误不伪装成业务拒绝 |
| 输出说明 | 字段、类型、是否可能为空、可引用范围 | 后续步骤的数据选择器 | 用户应能知道完成这个节点后会得到什么 |

## 7. 数据和表单的设计建议

流程提供统一的字段定义，表单是这些字段在某一步的呈现和编辑方式。建议用户可在制作表单时创建字段，不要求先完成独立的数据建模工作。

首批字段候选：短文本、长文本、数字、金额、日期、日期时间、单选、多选、是/否、人员、附件。明细表应作为重点讨论项：申请多个物品等场景通常需要多行“物品、数量、单位”，但仍应是用户自定义列，而不是内置物品申请表。

基本规则：

- 字段名称可改，已有引用应保持关联；删除被引用字段前展示受影响节点。
- 更改类型需要提示受影响的条件、表单和映射，不静默转换不兼容内容。
- 每个人工步骤分别配置可见、只读、可写和必填字段；流程发起人不自动获得所有字段的后续编辑权。
- 表单提交只写回该步骤允许编辑的字段，不覆盖其他步骤的数据。
- 流程字段表示当前业务值；节点提交结果保留当时的记录，避免后续修改抹掉审批依据。
- 条件分支中未执行节点的输出可能不存在；选择器提示并提供默认值或空值判断。
- 并行分支建议禁止同时写入同一流程字段；需要合并时在汇合后明确处理，避免最后写入者覆盖。
- 时间字段区分“日期”和“日期时间”，时间规则明确时区。

## 8. 人工任务和审批需要分清的行为

人工任务表示完成工作，审批表示作出决定。完成任务不能自动等同于审批通过。

建议首先支持：指定一个办理人；指定多个候选人，由一人领取处理；审批支持一个或多个人，并明确全部通过还是任一人通过。多数票、按比例、加签、委托、人员代理可后续设计。

“角色”表示谁承担某种业务职责，例如申请审核人，可跨行业自定义。是否由统一人员管理配置，还是每份流程先绑定角色，需要讨论；不建议直接在条件里写死某个部门。

审批结果建议区分：

| 结果 | 产品含义 | 后续行为 |
| --- | --- | --- |
| 通过 | 当前内容获认可 | 继续通过路径 |
| 拒绝 | 作出不批准决定 | 走拒绝路径；可结束，也可执行后续通知 |
| 退回修改 | 需要补充或修正后重审 | 回到明确的修改步骤，重新提交后重审 |
| 超时 | 截止或最长等待规则触发 | 按配置提醒/升级/超时路径，不默认为通过 |

建议第一版退回修改限定到当前审批对应的指定人工填写步骤，作为受控重提过程；暂不开放任意回跳制造无限循环。需要明确重提后复审哪些人、原意见如何保留、修改哪些数据会使先前审批失效。

人员为空、人员停用、没有人领取，均不能静默跳过。建议暂停并提示指定处理人；具体谁可处理此异常属于后续最小权限规则。

## 9. 分支、等待、失败与结束

- 条件分支首版采用“按顺序首个匹配”，始终配置默认路径。多个条件均命中时结果确定。
- 并行首版建议所有分支完成后继续。任一分支失败时显示其他分支状态，不能假装整体完成。
- 并行内的正常分支完成连接到汇合点；流程 End 节点表示整个运行结束，不能用同一种 End 同时表达这两个含义。
- 人工任务到期默认标记逾期并提醒；只有明确设置才进入其他处理路径。
- 时间等待和事件等待分别展示正在等什么、从何时开始、何时超时。
- 事件必须能关联到具体运行，例如订单编号；还需确定重复事件和节点进入等待前就收到事件时的规则。
- 自动重试用于可重试的系统动作失败。已经发出的消息或外部提交不能随意重复；未知执行结果应提示确认，避免重复业务动作。
- 提前结束需明确未完成任务取消，并保留已有结果。已经发生的外部动作不能因为结束流程就自动撤销。
- 用户主动取消、业务提前结束和技术执行失败应分别记录，即便本轮不做完整运行页面，也不能共用一个含糊的“失败”。

## 10. 保存、检查与发布的最小产品规则

建议允许不完整草稿自动保存。保存成功只意味着设计内容已保存，不意味着可以运行。

提供“检查流程”：点击问题能定位节点和字段。阻止发布的问题包括必填配置缺失、出口未连接、无效数据引用、审批人来源缺失、并行未汇合等；建议类问题与阻断错误分开展示。

发布形成可运行版本；新修改留在草稿，已开始的运行继续使用其启动版本。这样改图不会使运行中的任务含义突然变化。版本历史和发布权限本轮只确定原则，详细界面后续再做。

第一轮 Figma 中的“预览”含义尚未冻结。建议明确命名“模拟流程”：输入示例数据，查看条件命中、人员解析和待处理步骤；默认不发送真实通知、不调用真实外部动作。实际试运行应是另一个明确的操作，后续定义。

## 11. 下一版 Figma 应补充的界面

按讨论价值优先绘制，不一次绘制 15 套重复面板：

1. 节点库展开：7 类入口下的 15 项预设、说明和搜索。
2. 人工任务：基本信息、办理人来源、表单摘要、完成要求、截止提醒。
3. 审批：多人规则、通过/拒绝/退回出口，以及退回修改配置状态。
4. 通用数据选择器：固定值与引用切换、来源分组、类型不兼容和已删除引用。
5. 表单编辑区域：常用字段及明细表提案，区分可读、可写、必填。
6. 条件编辑器与分支画布：条件排序、默认路径、命中示例。
7. 等待与外部动作：时间/事件规则、输入输出映射、错误处理的不同布局。
8. 流程检查面板：错误定位、警告、允许保存但不可发布的状态。

本轮先把上述通用模式定下来，再用相同元素补齐其余预设。Task、看板、甘特图不绘制详细页面。

## 12. 需要讨论的决策与建议顺序

### 优先讨论：决定整体模型

| 问题 | 建议起点 | 为什么现在需要决定 |
| --- | --- | --- |
| 15 项清单是否接受，并行是否首版必须 | 按本稿讨论；若真实流程存在同时开展的工作，就保留并行 | 影响画布结构、数据引用和完成规则 |
| 表单与流程字段如何结合 | 统一字段，允许在表单中直接创建 | 影响所有输入、条件和输出配置 |
| 明细表是否首批支持 | 建议支持一层、用户自定义列 | 多物品申请、费用明细等通用业务可能离不开 |
| 办理人如何配置 | 固定人员、角色、人员字段引用；先支持单人及候选领取 | 决定能否表达真实业务分工 |
| 审批退回是否首批支持 | 支持限定目标的退回重提 | 影响数据修改和审批记录的有效性 |

### 接着讨论：决定具体交互

- 右侧分区折叠是否合适；复杂内容打开宽编辑区，还是独立全屏编辑？
- 一个流程首版是否只允许一个启动入口？建议先单入口，三类触发择一，避免重复启动及输入不一致。
- 多人审批先保留全部通过和任一通过，是否需要按顺序逐级审批？逐级审批建议用多个审批节点表达。
- 截止先按自然时长，还是必须支持工作日与节假日？后者需要统一工作日历。
- 外部服务首批具体有哪些？先根据真实来源确定连接和动作目录，不能只有一个泛用配置框却无法连接业务。
- 流程结果需要哪些业务名称？建议正常完成/业务终止为基础，结果标签由用户定义。

### 暂缓但记录依赖

Group 可见范围、复杂组织权限、任意循环与子流程、动态批量任务、复杂投票、代理和加签、补偿撤销、完整运行监控、任务管理页面、看板和甘特图。

暂缓不意味着把已有业务需求删除；若具体案例必须依赖某项，应调整本轮范围。

## 13. 用于评审设计的场景

以下是通用设计验证案例，不宣称来自此前 PDF：

1. 物品申请：填写多项申请 → 审批 → 执行交付 → 结束；验证明细、人员、数据传递和完成要求。
2. 退回修改：审批要求补充 → 修改指定字段 → 重新提交 → 再次审批；验证旧记录保留和任务重新生成。
3. 条件判断：金额高低走不同审批路径，空金额不能被当成零自动放行。
4. 并行准备：两个角色同时工作，全部完成再继续；验证汇合及字段写入冲突。
5. 定时巡检：到时发起，指定人员完成检查；验证时区、逾期与重复启动。
6. 等待外部结果：发出请求 → 等待对应业务编号的事件 → 成功继续或超时处理；验证关联与重复事件。
7. 修改设计：已发布流程产生新草稿，运行中的旧版本保持原规则。

评审通过标准：用户能解释每个节点何时开始、谁处理、使用哪些数据、何时完成、产生哪些输出、异常时去哪里；相同设置元素在不同预设中的含义保持一致。
